function addTwoNumbers (first, second)//define a function that can be called later
{
	return first + second;	
};

addTwoNumbers(1,2); //call the defined function


//var multiply = function(first, second) //define a function, assign reference to a variable
//{
//	return first * second;
//}

//multiply(3,4); //parenthesis runs code

//var moreMultiply = multiply; //no parenthesis passes a reference to function

//moreMultiply(5, 6); //now moreMultiply can be used as a function


//combining with objects

//var x = {};
//x.first = 'Dave';
//x.last = 'Terry'
//x.fullName = function()
//{
//	return this.first + ' ' + this.last;
//}

//x.fullName(); //parenthesis runs code


