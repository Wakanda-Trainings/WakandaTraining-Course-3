var theClass = ds.Person; // a class reference
theClass.getName();
//theClass.getScope();

//var nextNum = ds.Person.all().max('ID') + 1;
//theClass.setAutoSequenceNumber(nextNum);

//var theAtt = ds.Person.lastName; //reference to the attribute, not a value
//theAtt; //can get to a variety of properties

